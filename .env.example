# Payload
PAYLOAD_SECRET=your-secret-key-min-32-chars-long-change-in-production

# Database (Postgres) — use DATABASE_URI or DATABASE_URL
DATABASE_URI=postgresql://user:password@localhost:5432/zadanie_cms

# Optional: public URL for admin/emails
# NEXT_PUBLIC_SERVER_URL=http://localhost:3000
